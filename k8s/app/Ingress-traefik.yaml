apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: traefik-ingress
    namespace: hduhelp
spec:
  ingressClassName: traefik
  rules:
  - host: www.lqgege.byerer.online
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: gin-quickly-template-service
            port:
              number: 8080
